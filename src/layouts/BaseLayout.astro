---
import '../styles/global.css';
import { siteConfig } from '../site.config';

interface Props {
  title?: string;
  description?: string;
  overlayHeader?: boolean;
}

const { title, description = siteConfig.description, overlayHeader = false } = Astro.props;
const pageTitle = title ? `${title} — ${siteConfig.title}` : siteConfig.title;
---

<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 32 32%22><rect width=%2232%22 height=%2232%22 fill=%22white%22/><path d=%22M16 29 C16 29 3 20.5 3 12 C3 7.5 6.5 4 11 4 C13.5 4 15.5 5.5 16 7 C16.5 5.5 18.5 4 21 4 C25.5 4 29 7.5 29 12 C29 20.5 16 29 16 29 Z%22 fill=%22%23e02020%22/></svg>" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <meta name="description" content={description} />
    <title>{pageTitle}</title>
  </head>
  <body>
    <header class={overlayHeader ? 'site-header site-header--overlay' : 'site-header'}>
      <div class="wrap">
        <a class="site-title" href="/">{siteConfig.title}</a>
        <nav class="site-nav">
          {siteConfig.nav.map((item) => (
            <a href={item.href}>{item.label}</a>
          ))}
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="wrap">
        <span>&copy; {new Date().getFullYear()} {siteConfig.title}</span>
        <a href="/admin/">後台登入</a>
      </div>
    </footer>
  </body>
</html>
